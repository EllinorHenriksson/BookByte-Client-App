/**
 * App component for authenticated users.
 *
 * @returns {object} The jsx html template.
 */
function AppAuthenticated () {
  return (<div>
    <h2>App for anonymous users</h2>
  </div>)
}

export default AppAuthenticated
